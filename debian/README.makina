# clone the debian branch
# assemble upstream & plugins & packages divergence
debian/sync_with_debian.sh

# test build
debuild -b

# when down, sign & upload
debuild -k0x5616F8C2 -S -sa --lintian-opts -i
cd ..
dput nginx_1.7.2-1_source.changes
